name: Self Hosted CI Runner

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, win10-x64]

    steps:
      - name: cd C:/ and pwd
        shell: powershell
        run: |
          cd C:/dev-shells
          pwd